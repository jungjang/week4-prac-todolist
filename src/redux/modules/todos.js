//action value

//action creator

//initialState
const initialState = { todosList: [], detail: {} };
//reducer
const todos = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
//export
export default todos;
